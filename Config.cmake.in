@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

set(_deps "@PROJECT_DEPENDENCIES@")
foreach(dep IN LISTS _deps)
  find_dependency(${dep})
endforeach()

include("${CMAKE_CURRENT_LIST_DIR}/@PROJECT_NAME@Targets.cmake")
check_required_components("@PROJECT_NAME@")
